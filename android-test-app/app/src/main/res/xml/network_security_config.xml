<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Permite HTTP para pruebas locales (emulador/dispositivo físico). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
